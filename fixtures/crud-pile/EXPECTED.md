# crud-pile — ground truth
- receipts: 0 anywhere → every unit lists unreceipted rules
- duplicates (≥): cite-rule appears 4x across CLAUDE.md(x2), writing, writing-v2 — the two
  in-file CLAUDE.md copies pair (same-file, different sections OK); passive-voice pair
  CLAUDE.md vs writing is DIVERGED (scope differs: "in summaries" vs "anywhere"); word-count
  pair writing vs writing-v2 is a clean duplicate
- precedence: report must state which copy wins per its documented model
- dispositions: writing & writing-v2 = one-home (diverged/duplicated rules);
  review = turn-into-check (3 mechanically checkable rules) — note precedence: one-home
  outranks turn-into-check only where both fire on the SAME unit; review has no duplicates
  so turn-into-check stands; CLAUDE.md = one-home (participates in diverged pair)
- exit code: 1 (findings present)
